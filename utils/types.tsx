export type PermissionsObject = {
  granted: boolean;
  message: string;
};

export type ColorItemObject = {
  red: number;
  green: number;
  blue: number;
  hex: string;
};

export type PictureObject = {
  uri: string;
  width: number;
  height: number;
};
